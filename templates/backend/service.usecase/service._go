package service

import (
	model "{{PackagePath}}/server/service.usecase.model"
)

// IAppService is
type IAppService interface { {{range .Services}}
	{{PascalCase .Name}}(ctx map[string]interface{}, req model.{{PascalCase .Name}}Request) (*model.{{PascalCase .Name}}Response, error) {{end}}
}

// AppService is
type AppService struct {

}

{{range .Services}} 
// {{PascalCase .Name}} {{.Description}}
func (x *AppService) {{PascalCase .Name}}(ctx map[string]interface{}, req model.{{PascalCase .Name}}Request) (*model.{{PascalCase .Name}}Response, error) {

	return nil, nil
}
{{end}}