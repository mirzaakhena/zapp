package dao

import "{{PackageName}}/model"

// ISpaceAccountDao is
type ISpaceAccountDao interface {
	Create(dc model.DaoContext, bu model.SpaceAccount) error
	GetOne(dc model.DaoContext, spaceID, userID string) *model.SpaceAccount
	GetAll(dc model.DaoContext, req model.GetAllBasicRequest) ([]model.SpaceAccount, uint64)
	Delete(dc model.DaoContext, ID string) error
	Update(dc model.DaoContext, ID string, obj *model.SpaceAccount) error
}

// SpaceAccountDao is
type SpaceAccountDao struct {
}
