package model

import (
	"{{PackageName}}/shared/transaction" {{if HasTime .Fields}}
	"time"{{end}}
)

// {{PascalCase .Name}} is
type {{PascalCase .Name}} struct {
	transaction.BaseModel{{range .Fields}}
	{{.Name}} {{.DataType}} `json:"{{CamelCase .Name}}"` // {{end}}
}

// Create{{PascalCase .Name}}Request is
type Create{{PascalCase .Name}}Request struct { {{range .Fields}}
	{{.Name}} {{.DataType}} `json:"{{CamelCase .Name}}"` // {{end}}
}

// Update{{PascalCase .Name}}Request is
type Update{{PascalCase .Name}}Request struct { {{range .Fields}}
	{{.Name}} {{.DataType}} `json:"{{CamelCase .Name}}"` // {{end}}
}
